import type { TypographyProps } from '@rollout/ui-kit'

export type HeadingProps = {
  className?: string
  title?: React.ReactNode
  titleProps?: TypographyProps
  subTitle?: React.ReactNode
  subTitleProps?: TypographyProps
}
