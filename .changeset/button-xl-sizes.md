---
'@rollout/ui-kit': minor
---

Add `xl` (`h-10` `rounded-xl` `px-4` `text-sm`) and `icon-xl` (`size-10` `rounded-xl`) sizes to `Button`. Use these for primary mobile actions (40px) instead of inline `className` overrides.
